<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.f3app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".AddModifyNoteActivity"
            android:parentActivityName=".NoteActivity" />

        <activity
            android:name=".NoteActivity"
            android:parentActivityName=".AddModifyCourseActivity" />

        <activity android:name=".AddModifyAssessmentActivity"
            android:parentActivityName=".AssessmentActivity" />

        <activity
            android:name=".AssessmentActivity"
            android:parentActivityName=".AddModifyCourseActivity" />

        <activity android:name=".AddModifyCourseActivity"
            android:parentActivityName=".CourseActivity" />

        <activity
            android:name=".CourseActivity"
            android:parentActivityName=".AddEditTermActivity" />

        <activity
            android:name=".AddEditTermActivity"
            android:parentActivityName=".Main2Activity" />

        <activity
            android:name=".Main2Activity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>